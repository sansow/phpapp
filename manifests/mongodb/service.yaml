apiVersion: v1
kind: Service
metadata:
  name: mongodb-headless
  labels: { app: mongodb }
spec:
  clusterIP: None          # headless — gives each pod stable DNS
  selector: { app: mongodb }
  ports:
  - name: mongo
    port: 27017
